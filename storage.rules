rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TODO: tighten by path and auth roles before launch.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
