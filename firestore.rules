rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: tighten per-collection rules before launch.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
